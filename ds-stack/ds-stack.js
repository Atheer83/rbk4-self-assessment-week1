var Stack = function() {
    this._storage = [];
    this.add = function(){
      // write me
      Object.keys(this._storage[0])
    };
    this.remove = function() {
      // write me
      Object.keys(this._storage[this._storage.length - 1])
    };
  };